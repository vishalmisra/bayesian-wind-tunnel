# ⚠️ Not the canonical source

The paper **"The Workspace Survives Where the Circuit Dies: Routing Is Not
Computation"** now lives in its own repo, which is linked to Overleaf:

**Canonical home → https://github.com/vishalmisra/J-lens-revisited**  (Overleaf-linked)

The `main.tex`, `figures/`, and `references.bib` in *this* directory are a
**frozen snapshot** kept alongside the experiments. **Edit the paper in
`J-lens-revisited` (or Overleaf), not here** — changes made here will not reach
Overleaf and risk diverging from canonical.
